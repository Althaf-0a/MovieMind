# MovieMind

MovieMind is a full-stack AI movie recommendation system.

The project will combine a React frontend, a Python backend, and machine learning models to suggest movies based on user preferences.
